diff --git a/app/src/main/kotlin/org/fossify/phone/activities/MainActivity.kt b/app/src/main/kotlin/org/fossify/phone/activities/MainActivity.kt
--- a/app/src/main/kotlin/org/fossify/phone/activities/MainActivity.kt
+++ b/app/src/main/kotlin/org/fossify/phone/activities/MainActivity.kt
@@ -4,7 +4,7 @@ import org.fossify.phone.fragments.FavoritesFragment
 import org.fossify.phone.fragments.MyViewPagerFragment
 import org.fossify.phone.fragments.RecentsFragment
 import org.fossify.phone.helpers.OPEN_DIAL_PAD_AT_LAUNCH
-import org.fossify.phone.helpers.getSimPhonebookContacts
+import org.fossify.phone.helpers.getCachedSimPhonebookContacts
 import org.fossify.phone.helpers.RecentsHelper
 import org.fossify.phone.helpers.tabsList
 import org.fossify.phone.models.Events
@@ -19,7 +19,7 @@ class MainActivity : SimpleActivity() {
                     contacts.addAll(privateContacts)
                 }
             }
-            contacts.addAll(getSimPhonebookContacts())
+            contacts.addAll(getCachedSimPhonebookContacts())
             contacts.sort()
 
             try {
diff --git a/app/src/main/kotlin/org/fossify/phone/extensions/ContactSourceFilter.kt b/app/src/main/kotlin/org/fossify/phone/extensions/ContactSourceFilter.kt
--- a/app/src/main/kotlin/org/fossify/phone/extensions/ContactSourceFilter.kt
+++ b/app/src/main/kotlin/org/fossify/phone/extensions/ContactSourceFilter.kt
@@ -7,6 +7,7 @@ import org.fossify.commons.helpers.SMT_PRIVATE
 import org.fossify.commons.models.contacts.Contact
 import org.fossify.commons.models.contacts.ContactSource
 import org.fossify.phone.R
+import org.fossify.phone.helpers.getCachedSimPhonebookContactSources
 import org.fossify.phone.helpers.getSimPhonebookContactSources
 import org.fossify.phone.helpers.getSimPhonebookContacts
 
@@ -46,7 +47,7 @@ fun Context.getVisibleContactSourcesIncludingSim(): List<String> {
     val result = getVisibleContactSources().toMutableList()
     val ignored = config.ignoredContactSources
 
-    getSimPhonebookContactSources().forEach { source ->
+    getCachedSimPhonebookContactSources().forEach { source ->
         if (source.getFullIdentifier() !in ignored && source.name !in result) {
             result += source.name
         }
@@ -64,23 +65,81 @@ fun Context.loadVisiblePhoneContacts(
     callback: (ArrayList<Contact>) -> Unit,
 ) {
     val helper = ContactsHelper(this)
+    val resultsLock = Any()
+    var allowedVisibleNames: HashSet<String>? = null
+    var providerContacts: List<Contact>? = null
+    var simContacts: List<Contact>? = if (includeSim) null else emptyList()
+    var providerResultPublished = false
+    var simResultPublished = !includeSim
+
+    fun publishIfReady() {
+        val snapshot = synchronized(resultsLock) {
+            val sources = allowedVisibleNames
+            val contacts = providerContacts
+            if (sources == null || contacts == null) {
+                null
+            } else {
+                val normalContacts = contacts.filter { it.source in sources }
+                val sim = simContacts
+                when {
+                    !providerResultPublished -> {
+                        // Publish the complete ContactsProvider result immediately. A slow SIM
+                        // provider must never hold back first paint of the Phone contacts list.
+                        providerResultPublished = true
+                        if (sim != null) {
+                            simResultPublished = true
+                        }
+                        Pair(normalContacts, sim ?: emptyList())
+                    }
+
+                    sim != null && !simResultPublished -> {
+                        simResultPublished = true
+                        // Merge non-empty SIM results later. Avoid a second identical refresh for
+                        // empty SIM phonebooks.
+                        if (sim.isEmpty()) null else Pair(normalContacts, sim)
+                    }
+
+                    else -> null
+                }
+            }
+        } ?: return
+
+        val result = ArrayList(snapshot.first)
+        result.addAll(snapshot.second)
+        result.sort()
+        callback(result)
+    }
+
     helper.getContactSources { allSources ->
         val visibleNormalSources = getVisibleContactSources().toHashSet()
-        val allowedVisibleNames = filterAllowedContactSources(allSources)
+        val visibleAllowed = filterAllowedContactSources(allSources)
             .asSequence()
             .filter { it.name in visibleNormalSources }
             .mapTo(HashSet()) { it.name }
 
-        helper.getContacts(getAll = true, showOnlyContactsWithNumbers = true) { contacts ->
-            val result = ArrayList(contacts.filter { it.source in allowedVisibleNames })
-
-            if (includeSim) {
-                val visibleWithSim = getVisibleContactSourcesIncludingSim().toHashSet()
-                result.addAll(getSimPhonebookContacts().filter { it.source in visibleWithSim })
-            }
+        synchronized(resultsLock) {
+            allowedVisibleNames = visibleAllowed
+        }
+        publishIfReady()
+    }
 
-            result.sort()
-            callback(result)
+    helper.getContacts(getAll = true, showOnlyContactsWithNumbers = true) { contacts ->
+        synchronized(resultsLock) {
+            providerContacts = contacts
         }
+        publishIfReady()
+    }
+
+    if (includeSim) {
+        Thread {
+            val loadedSimContacts = getSimPhonebookContacts()
+            val visibleWithSim = getVisibleContactSourcesIncludingSim().toHashSet()
+            val visibleSimContacts = loadedSimContacts.filter { it.source in visibleWithSim }
+
+            synchronized(resultsLock) {
+                simContacts = visibleSimContacts
+            }
+            publishIfReady()
+        }.start()
     }
 }
diff --git a/app/src/main/kotlin/org/fossify/phone/helpers/SimPhonebook.kt b/app/src/main/kotlin/org/fossify/phone/helpers/SimPhonebook.kt
--- a/app/src/main/kotlin/org/fossify/phone/helpers/SimPhonebook.kt
+++ b/app/src/main/kotlin/org/fossify/phone/helpers/SimPhonebook.kt
@@ -13,6 +13,12 @@ import org.fossify.commons.models.contacts.ContactSource
 const val SIM_PHONEBOOK_SOURCE_TYPE = "android.sim.phonebook"
 private const val SIM_PHONEBOOK_SOURCE_PREFIX = "__fossify_sim_phonebook__:"
 
+@Volatile
+private var cachedSimPhonebookSources: List<SimPhonebookSource> = emptyList()
+
+@Volatile
+private var cachedSimPhonebookContacts: List<Contact> = emptyList()
+
 data class SimPhonebookSource(
     val subscriptionId: Int,
     val slotIndex: Int,
@@ -29,10 +35,11 @@ fun Contact.isSimPhonebookContact(): Boolean = source.isSimPhonebookSourceName()
 
 fun Context.getSimPhonebookSources(): List<SimPhonebookSource> {
     if (Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
+        cachedSimPhonebookSources = emptyList()
         return emptyList()
     }
 
-    return runCatching {
+    val sources = runCatching {
         val projection = arrayOf(
             SimPhonebookContract.ElementaryFiles.SUBSCRIPTION_ID,
             SimPhonebookContract.ElementaryFiles.SLOT_INDEX,
@@ -76,6 +83,19 @@ fun Context.getSimPhonebookSources(): List<SimPhonebookSource> {
 
         result.sortedBy { it.slotIndex }
     }.getOrDefault(emptyList())
+
+    cachedSimPhonebookSources = sources
+    return sources
+}
+
+fun Context.getCachedSimPhonebookContactSources(): List<ContactSource> {
+    val sources = cachedSimPhonebookSources
+    val multiple = sources.size > 1
+
+    return sources.map { source ->
+        val publicName = if (multiple) "SIM ${source.slotIndex + 1}" else "SIM"
+        ContactSource(source.sourceName, SIM_PHONEBOOK_SOURCE_TYPE, publicName, count = source.recordCount)
+    }
 }
 
 fun Context.getSimPhonebookContactSources(): List<ContactSource> {
@@ -88,9 +108,13 @@ fun Context.getSimPhonebookContactSources(): List<ContactSource> {
     }
 }
 
+fun Context.getCachedSimPhonebookContacts(): ArrayList<Contact> =
+    ArrayList(cachedSimPhonebookContacts)
+
 fun Context.getSimPhonebookContacts(): ArrayList<Contact> {
     val result = ArrayList<Contact>()
     if (Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
+        cachedSimPhonebookContacts = emptyList()
         return result
     }
 
@@ -103,6 +127,7 @@ fun Context.getSimPhonebookContacts(): ArrayList<Contact> {
         }
     }
 
+    cachedSimPhonebookContacts = result.toList()
     return result
 }
 
