# Signing

Fixed signing key for the regular filtered Fossify Phone replacement build (`org.fossify.phone`).

This is the initial key for the Phone filter line. Do not regenerate, replace, or rename `fossify-phone-filter.jks` in later versions. The official Fossify Phone release must be uninstalled before the first installation of this custom build because the signatures differ.
